<%= form_for Item.new, url: user_items_path(:user_id) do |i| %>
  <%= i.text_field :name, class: 'form-control', placeholder: "Enter new to-do list item" %>
  <div class="form-group">
    <%= i.submit "Save", class: 'btn btn-success' %>
  </div>
<% end %>
